// const { By, until } = require('selenium-webdriver');

async function interpret(driver, user, url) {
  try {

  } catch (e) {
    console.log(e);
    await driver.quit();
  }
}

module.exports = { interpret };
